version: 2
updates:
  # The workflow pins actions to commit SHAs, which only stay current if something
  # bumps them. Dependabot opens that PR and keeps the "# vX.Y.Z" comment in step.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
    commit-message:
      prefix: "chore"
